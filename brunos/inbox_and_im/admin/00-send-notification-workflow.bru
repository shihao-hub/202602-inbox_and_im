meta {
  name: 00 Send Notification Complete Workflow
  type: http
  seq: 0
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "testuser_{{$timestamp}}",
    "email": "testuser_{{$timestamp}}@example.com",
    "password": "Test123456",
    "full_name": "Test User"
  }
}

headers {
  User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:147.0) Gecko/20100101 Firefox/147.0
  Accept: application/json
  Content-Type: application/json
}

script:post-response {
  console.log("\n========================================");
  console.log("📋 发送通知完整工作流指南");
  console.log("========================================\n");

  const statusCode = res.getStatus();

  if (statusCode === 201) {
    const data = res.getBody();

    // 保存用户信息
    if (data.id) {
      bru.setVar("userId", data.id);
      console.log("✅ Step 1: 用户注册成功");
      console.log("   userId =", data.id);
    }
    if (data.access_token) {
      bru.setVar("userToken", data.access_token);
      console.log("   userToken 已保存\n");
    }

    console.log("接下来请按顺序运行以下测试：\n");
    console.log("2️⃣  auth/03-admin-login.bru");
    console.log("   （管理员登录，设置 adminToken）\n");
    console.log("3️⃣  admin/01-create-notification.bru");
    console.log("   （创建通知，设置 notificationId）\n");
    console.log("4️⃣  admin/06-send-notification-to-users.bru");
    console.log("   （发送通知给 userId=" + data.id + "）\n");
    console.log("========================================\n");

  } else {
    console.error("❌ 用户注册失败，状态码:", statusCode);
    bru.setNextRequest(false);
  }
}

tests {
  test("Status is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });

  test("Response has id", function() {
    const data = res.getBody();
    expect(data).to.have.property("id");
  });

  test("Response has access_token", function() {
    const data = res.getBody();
    expect(data).to.have.property("access_token");
  });
}
